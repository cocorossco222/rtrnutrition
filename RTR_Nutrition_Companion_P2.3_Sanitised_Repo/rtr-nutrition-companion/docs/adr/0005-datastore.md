# 0005 — Managed Postgres data core

**Status:** Accepted direction; production activation pending

Use managed Postgres with Supabase-compatible Auth/RLS semantics for UUID identity, event storage, snapshots and authorisation. The committed migration is a scaffold and requires provider-region, privacy, backup and RLS verification before production use.
