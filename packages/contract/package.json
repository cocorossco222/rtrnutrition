{
  "name": "@rtr/contract",
  "version": "2.0.0",
  "private": true,
  "description": "The sync and backup contract for the RTR Nutrition Companion: JSON Schema for the schema-2 export envelope, plus validation and canonical hashing.",
  "main": "index.js",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "engines": {
    "node": ">=22.22.2"
  }
}
